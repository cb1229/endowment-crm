# Database (Required)
# Get this from Vercel: Settings → Environment Variables → POSTGRES_URL
# Or from Supabase: Project Settings → Database → Connection String (URI mode)
POSTGRES_URL=

# Optional: Additional Supabase variables (automatically added by Vercel integration)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Vercel AI SDK (for future features)
OPENAI_API_KEY=
